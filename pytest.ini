[pytest]
markers =
    integration: marks tests as integration (deselect with '-m "not integration"')
    unit: marks tests as unit tests (select with '-m unit')
    version: marks tests related to version functionality
